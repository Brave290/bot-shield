"use client";

import { useEffect, useState } from "react";
import { DashboardShell } from "@/components/layouts/dashboard-shell";
import { toast } from "@/components/toast";
import { createClient } from "@supabase/supabase-js";

const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);

export default function SecurityCenter() {
  const [data, setData] = useState<any>(null);
  const [projectId, setProjectId] = useState("");
  const [policy, setPolicy] = useState({ profile: "balanced", threshold: 70, policyAction: "block", challenge_type: "behavioral" });
  const [webhook, setWebhook] = useState("");
  const load = async () => { const { data: auth } = await supabase.auth.getSession(); const response = await fetch("/api/security-center", { headers: { Authorization: `Bearer ${auth.session?.access_token || ""}` } }); if (response.ok) { const body = await response.json(); setData(body); setProjectId((current) => current || body.projects?.[0]?.id || ""); } };
  useEffect(() => { load(); }, []);
  const call = async (body: any) => { const { data: auth } = await supabase.auth.getSession(); const response = await fetch("/api/security-center", { method: "POST", headers: { Authorization: `Bearer ${auth.session?.access_token || ""}`, "Content-Type": "application/json" }, body: JSON.stringify(body) }); const result = await response.json(); if (!response.ok) return toast("error", result.error || "Action failed"); toast("success", result.warning || "Saved"); await load(); };
  if (!data) return <DashboardShell userType="user"><div className="min-h-[400px] animate-pulse rounded-2xl bg-slate-900" /></DashboardShell>;
  return <DashboardShell userType="user"><div className="space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-blue-400">Security control plane</p><h1 className="mt-2 text-3xl font-bold text-white">Security Center</h1><p className="mt-2 text-slate-400">Manage policies, keys, webhooks, and account security from one place.</p></div><section className="rounded-2xl border border-slate-800 bg-slate-900/50 p-6"><h2 className="text-xl font-semibold text-white">Policy builder</h2><div className="mt-4 grid gap-3 sm:grid-cols-4"><select value={projectId} onChange={(e) => setProjectId(e.target.value)} className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-3 text-sm text-white">{data.projects.map((p: any) => <option key={p.id} value={p.id}>{p.name}</option>)}</select><select value={policy.profile} onChange={(e) => setPolicy({ ...policy, profile: e.target.value })} className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-3 text-sm text-white"><option value="permissive">Permissive</option><option value="balanced">Balanced</option><option value="strict">Strict</option></select><select value={policy.policyAction} onChange={(e) => setPolicy({ ...policy, policyAction: e.target.value })} className="rounded-xl border border-slate-700 bg-slate-950 px-3 py-3 text-sm text-white"><option value="block">Block</option><option value="challenge">Challenge</option><option value="shadow">Shadow log</option></select><button onClick={() => call({ action: "save-policy", project_id: projectId, profile: policy.profile, threshold: policy.threshold, policy_action: policy.policyAction, challenge_type: policy.challenge_type })} className="rounded-xl bg-blue-600 px-4 py-3 text-sm font-medium text-white">Publish policy v{(data.policies?.filter((p: any) => p.project_id === projectId).length || 0) + 1}</button></div></section><section className="grid gap-6 lg:grid-cols-2"><div className="rounded-2xl border border-slate-800 bg-slate-900/50 p-6"><h2 className="text-xl font-semibold text-white">Key lifecycle</h2><p className="mt-2 text-sm text-slate-400">Rotation reveals a replacement once and gives the previous key a 24-hour migration window.</p><div className="mt-4 space-y-3">{data.projects.map((p: any) => <div key={p.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl bg-slate-950 p-4"><span className="text-sm text-white">{p.name}</span><button onClick={() => call({ action: "rotate-key", project_id: p.id })} className="rounded-lg border border-amber-500/30 px-3 py-2 text-xs text-amber-300">Rotate secret</button></div>)}</div></div><div className="rounded-2xl border border-slate-800 bg-slate-900/50 p-6"><h2 className="text-xl font-semibold text-white">Webhook subscriptions</h2><div className="mt-4 flex gap-2"><input value={webhook} onChange={(e) => setWebhook(e.target.value)} placeholder="https://your-app.com/botshield" className="min-w-0 flex-1 rounded-xl border border-slate-700 bg-slate-950 px-3 py-3 text-sm text-white" /><button onClick={() => call({ action: "create-webhook", endpoint_url: webhook })} className="rounded-xl bg-blue-600 px-4 py-3 text-sm text-white">Add</button></div><div className="mt-4 space-y-2">{data.hooks.map((hook: any) => <p key={hook.id} className="rounded-lg bg-slate-950 p-3 text-xs text-slate-400">{hook.endpoint_url} · {hook.active ? "active" : "paused"}</p>)}</div></div></section><section className="rounded-2xl border border-slate-800 bg-slate-900/50 p-6"><h2 className="text-xl font-semibold text-white">Security activity</h2><div className="mt-4 divide-y divide-slate-800">{data.events.length ? data.events.map((event: any) => <div key={event.id} className="flex justify-between gap-4 py-3 text-sm"><span className="text-slate-300">{event.event_type}</span><time className="text-xs text-slate-600">{new Date(event.created_at).toLocaleString()}</time></div>) : <p className="text-sm text-slate-500">No security events recorded yet.</p>}</div></section></div></DashboardShell>;
}
